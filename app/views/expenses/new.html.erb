<div>
<div>
<%= link_to group_expenses_path(params[:group_id]) do %>
  <%= image_tag ('back-icon.png'), class: "w-6 h-6" %>
  <% end %>
    <h3 class="ml-28">CREATE</h3>
</div>
  <h3 class="text-sky-400 text-xl font-bold mb-6 mt-4"> Add a new transaction</h3>
  <%= form_with url: group_expenses_path(params[:group_id]), scope: :expense do |form| %>
    <div>
    <%= form.label :Name %><br>
    <%= form.text_field :name, require: true %>
    </div>

    <div class="mt-2">
    <%= form.label :amount %><br>
    <%= form.number_field :amount, require: true %>
    </div>

    <%= form.submit "Submit" %>
    <%= link_to 'Back', group_expenses_path(params[:group_id]) %>
  <% end %>
</div> 